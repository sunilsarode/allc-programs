#include<bits/stdc++.h>
using namespace std;
int main(){
 vector<int> v;
    int n,val;
    cin>>n;
    for(int i=0;i<n;i++){
       cin>>val;  
       v.push_back(val);
       sort(v.begin(),v.end());
    } 
    return 0;

}

