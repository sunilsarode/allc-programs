#include<stdio.h>
int main()
{

    int t=0;
    scanf("%d",&t);
    while(t>0)
    {
        int len1=0,len2=0;

        scanf("%d %d",&len1,&len2);
        char arr[len1];
        char arr1[len2];
        scanf("%s",&arr);
        scanf("%s",&arr1);

        t--;
    }
    return 0;
}
