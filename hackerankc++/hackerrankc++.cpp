#include<bits/stdc++.h>
using namespace std;
int main(){

ios_base::sync_with_stdio(false);
cin.tie(NULL); 
cout.tie(NULL); 

    int n,val;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
       cin>>arr[i];  
       v.push_back(val);
       sort(v.begin(),v.end());
    } 
   for(auto i=v.begin();i!=v.end();i++)
    {
        cout<<*i<<" ";
    }
    return 0;

}

